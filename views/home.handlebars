<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>
</head>

<body>
  <h1>Lista de Productos</h1>
  <ul>
{{#each this}}
  <li>{{#if title}}{{title}}{{else}}No Title{{/if}} - {{#if price}}{{price}}{{else}}No Price{{/if}}</li>
{{/each}}
  </ul>
</body>

</html>
